# tui-cn

A minimal ui components built on top of [OpenTUI](https://opentui.com/) by opencode

## Setup

1. Clone the repo
2. Install the dependencies using `bun install`
3. To run the demo page, type `bun demo/demo.ts`
4. To run a template, type `bun template/<name_of_the file>`
